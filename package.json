{
  "name": "@luisfun/cloudflare-ai-plugin",
  "version": "0.2.1",
  "description": "This plugin enables you to code REST API or AI Gateway in the same way as the binding AI when using Cloudflare's Workers AI",
  "author": "Luis (https://github.com/LuisFun)",
  "license": "MIT",
  "keywords": [
    "cloudflare-ai",
    "workers-ai",
    "ai-gateway"
  ],
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": "./dist/index.js",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "fix": "npx @biomejs/biome check --apply .",
    "fix:unsafe": "npx @biomejs/biome check --apply-unsafe .",
    "patch": "npm run fix && npm run build && npm version patch && git push --follow-tags",
    "minor": "npm run fix && npm run build && npm version minor && git push --follow-tags",
    "major": "npm run fix && npm run build && npm version major && git push --follow-tags",
    "build:dts": "tsc && dts-bundle-generator ./dist/types/index.d.ts -o ./dist/index.d.ts --no-check && rimraf ./dist/types",
    "build:esm": "esbuild ./src/index.ts --outdir=./dist --bundle --platform=node --format=esm",
    "build": "rimraf ./dist && npm run build:dts && npm run build:esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "@cloudflare/ai": "^1.1.0",
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "@cloudflare/ai": "^1.1.0"
  }
}
